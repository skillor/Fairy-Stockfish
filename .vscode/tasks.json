{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Windows",
            "type": "shell",
            "command": "wsl sh -c \"make -j build COMP=mingw largeboards=yes nnue=no ARCH=x86-64 EXE=fairy-stockfish-largeboard_x86-64.exe && strip fairy-stockfish-largeboard_x86-64.exe && make clean\"",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}